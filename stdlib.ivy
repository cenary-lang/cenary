pure int addvals(int x, int y) {
  return x + y;
}

int identity(int k) {
  return k;
}

char myid(char k) {
  return k;
}

int myfun(int[] k) {
  return 5;
}

int fib(int n) {
  int a = 0;
  int b = 1;
  int sum = 6;
  while (n > 1) {
    sum = a + b;
    a = b;
    b = sum;
    n = n - 1;
  };
  return sum;
}
